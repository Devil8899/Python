1、什么是深浅拷贝 深拷贝和浅拷贝,
python中一切皆对象，像数字，字符串，元祖，
如果在内存中存储了这些元素，
那么这块内存中的值是不可以被修改的，
但是还存在一些可变对象，比如列表和字典，
他们所占有的内存空间是可以修改的，
有因为python中使用的是引用计数去节省内存。

总结一下：当你使用切片操作拷贝的时候，
我们修改了外层的列表，外层不随之改变，
但是 修改里层的子列表时，却会改变。
除此之外浅拷贝还有第二种方式，
及插入copy模块copy模块实现浅拷贝
首先我们删除列表，并插入模块


